import 'package:flutter/material.dart';

class EmilianoZapata extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title:const Text('Emiliano Zapata'),
      ),
      body: Center(
        child: Text('TIERRAS')
        )
      );
  }
}
