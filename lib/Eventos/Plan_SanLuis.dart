import 'package:flutter/material.dart';

class PlanSanLuis extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title:const Text('Plan de San Luis'),
      ),
      body: Center(
        child: Text('FUCHI PORFIRIOS')
        )
      );
  }
}
